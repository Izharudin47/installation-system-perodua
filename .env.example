# Django settings
SECRET_KEY=change-this-to-a-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# MySQL database settings
DB_NAME=installation_system          # must match the DB you created in MySQL
DB_USER=install_user                 # or your MySQL username
DB_PASSWORD=YOUR_MYSQL_PASSWORD_HERE
DB_HOST=localhost
DB_PORT=3306

# Optional: single URL form (for deployment, can leave empty locally)
# Example:
# DATABASE_URL=mysql://install_user:YOUR_MYSQL_PASSWORD_HERE@localhost:3306/installation_system

# JWT
JWT_SECRET=change-this-jwt-secret-or-reuse-secret-key

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Geocoding (optional for now)
GEOCODING_API_KEY=
GEOCODING_PROVIDER=locationiq